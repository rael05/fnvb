<%= form_with(model: announcement) do |form| %>
  <% if announcement.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(announcement.errors.count, "error") %> prohibited this announcement from being saved:</h2>

      <ul>
        <% announcement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label 'PDF de la convocatoria en español', style: "display: block" %>
    <% if announcement.pdf_es_file.present? %>
      Arcivo actual: <%= announcement.pdf_es_file.filename %> <br>
    <% end %>
    <%= form.file_field :pdf_es_file %>
  </div>

  <div>
    <%= form.label 'PDF de la convocatoria en ingles', style: "display: block" %>
    <% if announcement.pdf_en_file.present? %>
      Arcivo actual: <%= announcement.pdf_en_file.filename %> <br>
    <% end %>
    <%= form.file_field :pdf_en_file %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
