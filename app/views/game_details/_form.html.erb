<%= form_with(model: game_detail) do |form| %>
  <% if game_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game_detail.errors.count, "error") %> prohibited this game_detail from being saved:</h2>

      <ul>
        <% game_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :game_id, style: "display: block" %>
    <%= form.text_field :game_id %>
  </div>

  <div>
    <%= form.label :game_time, style: "display: block" %>
    <%= form.text_field :game_time %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
