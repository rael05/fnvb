<div id="<%= dom_id tournament %>">
  <p>
    <strong><%= t(:name) %>:</strong>
    <%= tournament.name %>
  </p>

  <p>
    <strong><%= t(:description) %>:</strong>
    <%= tournament.description %>
  </p>

  <p>
    <strong><%= t(:international) %>:</strong>
    <%= tournament.international %>
  </p>

  <p>
    <strong><%= t(:image) %>:</strong>

    <% if tournament.image %>
      <%= image_tag tournament.image_url%>
    <% else %>
      <%= t(:without_image) %>
    <% end %>
  </p>

</div>
